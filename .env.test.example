NODE_ENV=test
APP_ENV=test
APP_URL=http://127.0.0.1:3000
LOG_LEVEL=silent
DATABASE_DRIVER=pg
DATABASE_URL=postgresql://test:test@127.0.0.1:5432/estate_sales_test
DIRECT_URL=postgresql://test:test@127.0.0.1:5432/estate_sales_test
CRON_SECRET=test-only-cron-secret-at-least-32-characters
# Live-only checks use an isolated non-production Neon branch and Private Blob store.
# They are intentionally not required by pnpm verify or pull-request CI.
VERCEL_BENCHMARK_URL=
